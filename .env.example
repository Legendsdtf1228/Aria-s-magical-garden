# Local secrets only — never commit. Copy to `.env` and fill in if needed.
# Prefer `npx wrangler login` (OAuth) for Workers AI CLI generation.

# Optional overrides (Wrangler OAuth is used when these are unset):
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=
